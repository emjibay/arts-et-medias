{%- from 'macros/featured-piece.njk' import featuredOrganization -%}


{# FIXME: figure out better way to get widget data #}
{% set hasWidget = data.widget._pieces.length > 0 %}

{%- if hasWidget -%}
  {%- if data.widget.isSponsored -%}
    {%- set label = __('organizations|sponsored') -%}
  {%- else -%}
    {%- set label = __('organizations|featured') -%}
  {%- endif -%}

  {%- set piece = data.widget._pieces[0] -%}
  {{ featuredOrganization(piece, label) }}
{%- endif -%}
