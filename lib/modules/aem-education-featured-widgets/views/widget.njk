{%- from 'macros/featured-piece.njk' import featuredEducation -%}


{# FIXME: figure out better way to get widget data #}
{% set hasWidget = data.widget._pieces.length > 0 %}

{%- if hasWidget -%}
  {%- if data.widget.isSponsored -%}
    {%- set label = 'Sponsored Academic Program' -%}
  {%- else -%}
    {%- set label = 'Featured Academic Program' -%}
  {%- endif -%}

  {%- set piece = data.widget._pieces[0] -%}
  {{ featuredEducation(piece, label) }}
{%- endif -%}
