{% from 'macros/bibliography.njk' import apaBib, chicagoBib, mlaBib %}
{% from 'macros/footnote.njk' import apaFootnote, chicagoFootnote, mlaFootnote %}

<section>
  <h2 class="h1">{{ __('books|sidebar|heading') }}</h2>

  <h3 class="h2">{{ __('books|sidebar|footnoteHeading') }}</h3>
  <h4 class="h3">{{ __('books|sidebar|styles|apa') }}</h4>
  <p>{{ apaFootnote(data.piece) }}</p>
  <h4 class="h3">{{ __('books|sidebar|styles|chicago') }}</h4>
  <p>{{ chicagoFootnote(data.piece) }}</p>
  <h4 class="h3">{{ __('books|sidebar|styles|mla') }}</h4>
  <p>{{ mlaFootnote(data.piece) }}</p>

  <h3 class="h2">{{ __('books|sidebar|bibliographyHeading') }}</h3>
  <h4 class="h3">{{ __('books|sidebar|styles|apa') }}</h4>
  <p>{{ apaBib(data.piece) }}</p>
  <h4 class="h3">{{ __('books|sidebar|styles|chicago') }}</h4>
  <p>{{ chicagoBib(data.piece) }}</p>
  <h4 class="h3">{{ __('books|sidebar|styles|mla') }}</h4>
  <p>{{ mlaBib(data.piece) }}</p>

  <p class="citation-warning">{{ __('books|sidebar|warning') }}</p>
</section>
